const axios = require("axios");
const FormData = require("form-data");

class ONEDG {
    constructor(apiKey) {
        this.apikey = apikey;
        this.api_URL = "";
    }
    async addOrder(options) {
        console.log(test);
    }
}
